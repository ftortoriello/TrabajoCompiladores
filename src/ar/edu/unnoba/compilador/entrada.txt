# Este ejemplo anduvo pero hay que ponerle tres punto y coma al final (porque de la
# forma que está hecho ahora, el while, el if y funca = false son todas "sentencias",
# y las sentencias estan diseñadas para llevar siempre punto y coma.)

variable X is float;

main is
    X = read_float();
    writeln("HOLA CHEE!");
    while getUno() == 1 do
        if radio < 0 then
        begin
            X = .55;
            altura = 10 - 10 + 20 * 4 / 2;
            ancho = getAncho();
            imprimir(msj);
        end;;
end.

(* (x and y) or not z *)
