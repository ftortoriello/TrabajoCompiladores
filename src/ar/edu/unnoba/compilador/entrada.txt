# Este ejemplo anduvo, pero en ciertos casos hay que poner varios punto y coma al final
# (porque de la forma que está hecho ahora, el while y el if son "sentencias",
# y las sentencias estan diseñadas para llevar siempre punto y coma.)

variable X is float;

function doble(n is integer) return integer
begin
    variable resultado is integer = n * 2;
    return resultado;
end;

main is
    X = read_float();
    writeln("HOLA CHEE!");
    while getUno() == 1 do
        if radio < 0 then
        begin
            variable ancho is integer;
            X = .55;
            altura = 10 - 10 + 20 * 4 / 2;
            ancho = getAncho();
            imprimir(msj);
        end;;
end.

(* (x and y) or not z *)
